# Approaches

## Summary Table

| # | Approach | Example from white box test | Tried? | White box | Black box | Successful? | Comments |
|---|----------|-----------------------------|--------|-----------|-----------|-------------|----------|
| 1 | Adding punctuation | cc1 has `等上了高中後，我一定要玩樂團` with comma; transcription `登上了高中後我一定要外月退` has none. Also `"來自壁櫥，我的愛"` has quotes and comma in ground truth. | YES | 79.0% (was 78.2%) | 70.9% (unchanged) | NO | LLM-based punctuation hurt scores badly (78.2%→71.4%); rule-based was safe but marginal (+0.8% white box only). Ceiling is very low: ~16 punct chars in ground truth, most in lines with other content mismatches. Black box got zero benefit. |
| 2 | Building a homophone dictionary from reference subtitles and replacing words in transcription | `文化機` appears in seg 5 and `文化界` in seg 34; both should be `文化祭` (from reference). If we learn 機→祭 from the reference alignment in one segment, we can apply it to fix `界→祭` in the other. Also `反碎→粉碎` appears in both seg 46 and 47. | YES | 78.2% (unchanged) | 70.9% (unchanged) | NO | Tested 3 implementations: (1) Reference alignment dictionary — alignment quality threshold filters out short segments, and reference vocabulary differs from spoken vocabulary (learns 機→節 instead of 機→祭); (2) Post-hoc dictionary from LLM corrections — found 4 patterns but none transferred to other segments since errors are unique per segment; (3) Reference vocabulary matching with jieba — massive false positives (75.4% white box, 69.5% black box) because Chinese has too many homophones. Core issues: reference subs use different vocab than spoken dialogue, and the LLM+per-segment alignment already catches the main cross-segment examples. |
| 3 | Use more transcription context (wider window) for LLM | Seg 33 `英雄的社交障礙權` should be `陰沉的社交障礙者`. The LLM currently sees ±3 lines (segs 30–36), but seg 23 mentions `吉他英雄` (the username). With wider context, the LLM could realize `英雄` in seg 33 is wrong — the speaker is describing social recluses, not heroes. Similarly seg 11 `外月退` → `玩樂團`: seeing seg 7 `樂團成員` in context would reinforce that this is about forming a band. | YES | 78.2% (was 77.8%) | 70.9% (unchanged, ±0.2% noise) | NO | Tested ±5, ±7, ±10 windows and structured "broader context" section. White box consistently +0.4% from one new fix (`避除→壁櫥` in seg 17). But neither target example improved: `外月退→玩樂團` fails pinyin filter (外≠玩), `英雄→陰沉` is not a homophone so correctly blocked. Black box unchanged or slightly worse (70.6–70.9% across runs vs 71.1% baseline, within LLM noise). Wider context may cause more aggressive false corrections. |
| 4 | Use more reference context for better scene/narrative awareness | Seg 34 `我的樂團在文化界也糟了正` → ground truth `我的樂團在文化祭演奏了這⋯`. Currently only 5 nearest reference lines by time overlap are provided. Providing more reference lines (or a broader time window) gives the LLM fuller narrative context: the story is about performing at a cultural festival. Similarly segs 46–47 `反碎你頭開頭` → `粉碎你的頭蓋骨`: broader reference context about death metal song lyrics would help. | YES | 78.2% (unchanged) | 70.9–71.1% (unchanged, within noise) | NO | Tested 3 variations: (1) Simply increasing ref-count from 5→10 and window 5s→10s — no improvement, slightly worse black box (70.9%); (2) Structured broad+close refs with filtered quality lines — same as baseline (78.2%/71.1%) but introduced false correction (seg 33 權→圈, ground truth 者); (3) Unfiltered broad refs — worse (77.8%/70.9%), lost seg 17 壁櫥 fix because noisy danmaku confused LLM into suggesting 避除→櫥櫃 instead. Core issue: target errors are NOT homophone errors (糟了正→演奏了這: 糟≠演, 正≠這; 頭開頭→頭蓋骨: 開≠蓋, 頭≠骨), so the pinyin filter correctly rejects them regardless of reference context quality. More reference lines add noise from danmaku/comments without enabling new fixes. |
| 5 | Switch to pypinyin-g2pw with GPU acceleration for context-aware polyphone disambiguation | Current `pypinyin.lazy_pinyin` uses dictionary lookup and returns the default reading for polyphones (多音字), ignoring context. E.g., `樂` defaults to `lè` but should be `yuè` in `樂團` (band). If the homophone filter compares a character against `樂` expecting `yuè` but gets `lè`, a valid fix is falsely rejected. pypinyin-g2pw uses a BERT-based G2PW model that reads surrounding context to pick the correct pronunciation, and supports GPU acceleration for fast inference. This improves the accuracy of `is_homophone` and `similar_pinyin` — the foundation all pinyin-dependent approaches (#8, #9, #10, #16) rely on. | BLOCKED | — | — | — | Cannot install pypinyin-g2pw — no network access in environment. Package not pre-installed. |
| 6 | Segment deduplication — remove duplicate and near-duplicate ASR segments | Transcription seg 38 and seg 40 are both `快找我聊天` (exact duplicate). Seg 46 `看我怎麼反碎你頭開頭` fully contains seg 47 `我怎麼反碎你頭開頭` (near-duplicate suffix). Seg 44 `看我` is a fragment of seg 46. CC has none of these duplicates. Using SequenceMatcher or simple substring detection on consecutive segments, remove exact duplicates and segments that are substrings of nearby segments. | YES | 80.7% (+2.5%) | 71.4% (+0.5%) | YES | Implemented with exact duplicate removal + SequenceMatcher ratio ≥ 0.9 threshold. White box removes 2 segments (seg 40 exact dup, seg 47 near-dup ratio=0.95). More aggressive approaches (substring removal, lower thresholds) hurt black box significantly — short fragments and substring matching produced false positives on legitimate repetitions. Conservative approach (only exact + 0.9 similarity) is safe for both test sets. |
| 7 | Number format normalization (Chinese numerals → Arabic) | Seg 9 `整整三年` → cc `整整3年`. Seg 2 `六小時` → cc `6小時`. Seg 42 `二年接班` → cc `2年A班`. Professional CC conventions (Netflix Traditional Chinese Style Guide) use Arabic numerals for specific quantities. | TOBETESTED | est. 81.6% (+0.5% on top of #5) | — | — | Small but reliable gain. Only converts numerals in number+counter patterns (三年→3年, 六小時→6小時) to avoid false positives. |
| 8 | Pinyin-augmented LLM prompt — include pronunciation in prompt | Seg 10 `登上了高中` → cc `等上了高中`. Both 登(dēng) and 等(děng) have pinyin "deng" — the LLM should catch this but doesn't because it doesn't see the pronunciation. Research (PY-GEC 2024, PYGEC Interspeech 2025, Pinyin Regularization 2024) consistently shows that adding pinyin to LLM prompts significantly improves Chinese ASR error correction. By showing the LLM `登上了高中 [dēng shàng le gāo zhōng]`, it can notice 登=等 immediately. | TOBETESTED | est. +0.4% | — | — | Research-backed technique. Should help the LLM catch homophones it currently misses because it doesn't "hear" the pronunciation from text alone. Low risk since the pinyin filter still validates. |
| 9 | Expand similar_pinyin to allow (similar_initial AND similar_final) | Seg 8 `沒找到半個` → cc `沒交到半個`. 找(zhǎo) and 交(jiāo): initial zh/j are in confusion group {zh,z,j}, final ao/iao are in confusion group {ao,iao}. Current code requires EITHER (exact initial + similar final) OR (similar initial + exact final), so this pair is rejected. Allowing (similar initial + similar final) would catch these genuine ASR confusions. | TOBETESTED | est. +0.2% | — | — | Needs careful tuning — allowing both to be "similar" could increase false positives. Mitigation: only allow when the LLM specifically suggested the fix (high confidence from context). |
| 10 | Second LLM pass for non-homophone ASR corrections with reference validation | Seg 34 `也糟了正` → cc `演奏了這⋯`; seg 11 `外月退` → cc `玩樂團`; seg 33 `英雄的社交障礙圈` → cc `陰沉的社交障礙者`. These are NOT homophones so the pinyin filter correctly blocks them. But they ARE genuine ASR errors where the reference strongly indicates the correct text. A second pass with a different prompt (focused on semantic correction, not homophones) could fix these, using the reference as validation. Skip pinyin filter for this pass; instead validate that the corrected phrase appears in or is strongly supported by reference text. | TOBETESTED | est. +2.0% | — | — | Highest potential impact of remaining approaches (~+2% from multiple non-homophone fixes). Risk: false positives from aggressive LLM corrections. Mitigation: only apply when reference explicitly contains the corrected phrase or a close variant. |
| 11 | Allow variable-length corrections (relax len(wrong)==len(correct) constraint) | Seg 46 `粉碎你頭開頭` → cc `粉碎你的頭蓋骨`. The correct text is 4 chars `的頭蓋骨` replacing 3 chars `頭開頭`. Current code rejects all corrections where wrong and correct have different character counts. Relaxing this for reference-validated corrections would catch insertion/deletion ASR errors. | TOBETESTED | est. +0.5% | — | — | Only safe in combination with approach #10 (reference-validated corrections). Without reference validation, allowing length changes risks catastrophic false positives. |
| 12 | Named entity detection from reference | Seg 43 `後頭同學` → cc `後籐同學`. 後藤/後籐 (gotō) is a common Japanese surname. The reference (seg 46) contains `後藤同學`. ASR wrote `後頭` as phonetic approximation. Detect named entities (person names, place names, etc.) in reference subs and match against ASR output using phonetic similarity. | TOBETESTED | est. +0.2% | — | — | Covers proper names broadly (Japanese surnames like 後藤/佐藤, place names, character names). Cross-reference named entities found in reference text with phonetically similar but incorrect text in ASR output. Very low false positive risk since entities are validated against reference. |
| 13 | Mixed-language content handling (English/numbers in Chinese text) | Seg 42 `二年接班` → cc `2年A班`. ASR cannot produce English letters, so it writes phonetic approximations: `接` (jiē) for "A" (the letter). Reference (seg 46) has `2年A班的後藤同學`. Detect from reference when English letters or alphanumeric content appears in the subtitle, and substitute matching segments. | TOBETESTED | est. +0.2% | — | — | Specific to anime/media content that mixes languages. Detect patterns like `X年X班` in reference and replace phonetic approximations with the actual mixed-language text. |
| 14 | Context-aware pronoun disambiguation (他/她/它) | Seg 49 `快把他忘掉` → cc `快把它忘掉`. 他(he) and 它(it) are both tā — perfect homophones. Current `fix_common_homophones` only works when reference explicitly uses the target pronoun. Here the reference says `快忘掉 快忘掉` (no pronoun). Add LLM-based pronoun disambiguation using semantic context: seg 48 mentions `要跑出來` referring to 陰影 (shadow/memory), so `它` (it) is correct, not `他` (he). | TOBETESTED | est. +0.2% | — | — | Safe approach since 他/她/它 are perfect homophones (no pinyin filter issue). The LLM just needs to determine the referent from context. Could be added as a specific prompt instruction. |
| 15 | Inject on-screen text/annotations from reference subtitles | CC has `（恭喜畢業）`, `（全校學生都超high的）`, `（我也跟輕音社的人一起彈了）`, `（有沒有會彈吉他的人）` — parenthetical on-screen text that ASR cannot capture (not spoken). Reference subs near these timestamps contain matching content (e.g., ref `全校的學生都熱情高漲` near cc's `全校學生都超high的`). Detect reference-only content at timestamps where ASR has no output, and inject as parenthetical segments. | TOBETESTED | est. +3-5% | — | — | High potential — ~40 missing characters of on-screen text. But complex: reference wording differs from CC (translated subtitles vs actual on-screen text), so exact matching is hard. May need LLM to generate CC-style parenthetical text from reference. Risk of adding wrong content if reference timestamps don't align well. |
| 16 | Improve LLM prompt examples to target commonly missed homophone patterns | Current prompt examples include 過重→國中, 文化機→文化祭, etc. But the LLM misses 登→等 (dēng→děng, tone-only difference in a common word pair). Add more examples targeting: (1) tone-only confusions (登/等, 嘛/麻), (2) common word-pair confusions the LLM currently misses, (3) explicit instruction to consider each character individually. Also add negative examples matching the specific false positives seen (e.g., 樂團≠樂隊, 試彈≠翻彈). | TOBETESTED | est. +0.2-0.4% | — | — | Low risk, easy to implement. May help the LLM catch patterns it currently overlooks. Combined with approach #8 (pinyin in prompt), this addresses the LLM's blind spots for valid homophones. |

## Priority and Dependency Analysis

### Recommended implementation order (by confidence and impact):

1. **#5 pypinyin-g2pw** — Foundational improvement to pinyin accuracy. Implement first as it benefits all pinyin-dependent approaches.
2. **#6 Segment deduplication** — Highest confidence, highest single-approach impact (+2.9%). No risk of false positives.
3. **#7 Number normalization** — High confidence, easy to implement (+0.5%). No dependencies.
4. **#8 Pinyin-augmented prompt + #16 Improved examples** — Research-backed, low risk. Implement together.
5. **#14 Pronoun disambiguation** — Safe (perfect homophones), small but reliable gain.
6. **#9 Expand similar_pinyin** — Medium confidence, needs careful testing for false positives.
7. **#10 Non-homophone correction + #11 Variable-length** — Highest remaining potential but highest risk. Implement last with strong reference validation.
8. **#12 Named entities + #13 Mixed-language** — Narrow but reliable for anime content.
9. **#15 On-screen text injection** — Highest theoretical impact but most complex and error-prone.

### Cumulative impact estimate (white box):

| After approach | Estimated similarity |
|---|---|
| Baseline (current) | 78.2% |
| +#5 pypinyin-g2pw | BLOCKED |
| +#6 dedup | 80.7% (actual) |
| +#7 numbers | 81.6% |
| +#8+#16 pinyin prompt | ~82.0% |
| +#9 expanded pinyin filter | ~82.2% |
| +#12+#13+#14 names/mixed/pronouns | ~82.8% |
| +#10+#11 non-homophone pass | ~85.0% |
| +#15 on-screen text | ~87-89% |

### White box error categorization:

- **Duplicate/fragment segments**: ~19 extra chars → Approach #6
- **Number format**: ~3 chars → Approach #7
- **Missed homophones** (登→等, 找→交, 他→它): ~6 chars → Approaches #5, #8, #9, #14, #16
- **Non-homophone ASR errors** (英雄→陰沉, 也糟了正→演奏了這, 外月退→玩樂團, 頭開頭→頭蓋骨): ~20 chars → Approaches #10, #11
- **Named entity/foreign errors** (後頭→後籐, 接班→A班): ~4 chars → Approaches #12, #13
- **Missing on-screen text** (恭喜畢業, etc.): ~40 chars → Approach #15
- **Other** (missing words at segment start/end, punctuation, synonym differences): ~20 chars → Partially addressed by #1 (already tried)

## Approach #6 Results: Segment Deduplication

### What was done
Added a post-processing `deduplicate_segments()` step after all LLM corrections. Uses `difflib.SequenceMatcher` to detect duplicates within a sliding window of 3 consecutive segments. Two removal criteria:
1. **Exact duplicates**: segments with identical text — remove the later one
2. **High similarity** (ratio ≥ 0.9): near-duplicate segments — remove the shorter one

### Key numbers
- **White box**: 78.2% → 80.7% (+2.5%). Removes seg 40 `快找我聊天` (exact dup of seg 38) and seg 47 `我怎麼粉碎你頭開頭` (0.95 similar to seg 46 `看我怎麼粉碎你頭開頭`)
- **Black box**: ~70.9% → ~71.4% (+0.5%)
- White box original transcription similarity also improved: 75.9% (was ~72% before dedup of raw transcription is also visible in comparison)

### Findings and quality notes
- **Conservative thresholds are essential**: Substring removal and lower similarity thresholds (0.7-0.8) caused 2-3% drops in black box score by removing legitimate speech repetitions
- **Short fragment removal is risky**: Removing segments ≤ 2-3 chars that are substrings of nearby segments helped white box but hurt black box — short segments can be legitimate
- **Seg 44 `看我` not removed**: This 2-char fragment could be deduped but the safe threshold doesn't catch it. Removing it helps white box (+0.8%) but hurts black box
- **LLM noise**: Results vary ±0.3% between runs due to LLM non-determinism. The dedup improvement is clearly above noise level for white box but borderline for black box
